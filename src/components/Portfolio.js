import React, { useState } from 'react';
import Navbar from './navbar'

const Portfolio = () => {
    return(
        <div>
            <Navbar />
        Test text
        </div>
    );
};

export default Portfolio;